// ---------------------------------------------------------------------------
// Domain types — re-exported from the canonical source
// ---------------------------------------------------------------------------

export type {
  Project,
  ProjectSummary,
  Task,
  TaskSummary,
  ActivityLog,
  TaskStatus,
  EffortLevel,
  ImpactLevel,
  TaskCategory,
  EntityType,
  ActivityAction,
} from '@domain/entities';

export {
  TASK_STATUSES,
  EFFORT_LEVELS,
  IMPACT_LEVELS,
  TASK_CATEGORIES,
  ENTITY_TYPES,
  ACTIVITY_ACTIONS,
} from '@domain/entities';
